# toolbox

日常で使うツールやアイデアを管理するリポジトリ。

## 構成

```
toolbox/
├── tools/       # ツール（ディレクトリ単位で管理）
│   └── <tool-name>/
└── scripts/     # 単発スクリプト
```

## ツールの追加

`tools/` 配下にディレクトリを作成し、README.md と実装を配置する。

```bash
tools/
└── my-tool/
    ├── README.md
    └── main.py
```
