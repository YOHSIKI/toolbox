# Web / スケジューラ
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
jinja2>=3.1,<4.0
python-multipart>=0.0.9,<1.0
APScheduler>=3.10,<4.0

# 設定
pydantic>=2.6,<3.0
pydantic-settings>=2.2,<3.0

# 時刻
tzdata>=2024.1

# シークレット復号
cryptography>=42,<46
PyYAML>=6.0,<7.0

# HTTP（Chrome TLS 指紋互換、予約 API 用）
curl_cffi==0.15.0
# HTTP（Discord Webhook 等の付随通信）
httpx>=0.27,<1.0

# キャッシュ
cachetools>=5.3,<7.0
